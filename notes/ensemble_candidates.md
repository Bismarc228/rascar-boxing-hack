# Ensemble Candidate Registry

Purpose: keep only independent, non-dead branches that may be useful for a
future ensemble. This is not a submit queue. A row can be kept here with weak
public transfer if it provides a different signal for timing, count, identity,
or gating.

Current hard guardrail: no default Kaggle uploads. At most two more submissions
today, only if offline evidence clearly beats the current public anchor.

## Current Anchor

| Branch | Evidence | Artifact | Ensemble role | Submit status |
| --- | --- | --- | --- | --- |
| yolo26l public base + sequence snap4 only on `agn_038` | Public `0.16461`, current best. Keeps public-proven base and edits only the one confirmed good sequence video. | `submissions/hybrid_yolo26l_best_agn038_seq_tcn_snap4_rootcount088_OFFLINE_CANDIDATE.csv`; root `submission.csv` points here. | Main anchor / fallback. | Already submitted; do not resubmit. |

## Keep For Ensemble

| Branch | Evidence | Artifact | Ensemble role | Submit status |
| --- | --- | --- | --- | --- |
| yolo26l same-sum public base | OOF validation rows `0.365177`; public `0.13849`. Precision-oriented count control transferred better than denser local winners. | `data/processed/validation_rows/yolo26l_samesum_w4_am02_thr085_same10_cross4_rootrate088.csv`; `submissions/yolo26l_samesum_w4_am02_thr085_same10_cross4_rootrate088_OFFLINE_CANDIDATE.csv`. | Conservative timing/count base. | Already submitted; not enough alone now. |
| sequence TCN snap4 rootcount088 | Full OOF `0.379682`, better than yolo26l validation, but full public submit was only `0.12712`. Public failure is video-local, not total death. | `data/processed/validation_rows/seq_tcn_snap4_rootcount088_oof.csv`; `submissions/seq_tcn_yolo26x_witness_3seed_thr06_nms10_cross2_snap4_rootcount088_OFFLINE_CANDIDATE.csv`. | Timing witness and replacement pool, never direct full replacement. | Already submitted full and failed; keep only as per-video source. |
| yolo26l/sequence threshold-gated hybrid | OOF gate sweep reached `0.395013`; matching test gate selects `agn_038,agn_062,agn_063` and rejects `agn_037`. Expected public-neutral beyond anchor but plausible private lift. | `submissions/hybrid_yolo26l_best_seq_tcn_snap4_gate_oof395_test_OFFLINE_CANDIDATE.csv`. | Private-risk per-video replacement set. | Not submitted under current cap. |
| crop-motion rescored yolo26l context | Full validation `0.365177 -> 0.368244`; improves time/fighter but increases FP and rewrites many selected events. Test artifact has `707` clear rows, `+11` versus the yolo26l base, with `420` rowwise frame/fighter/hand/target/clear differences. Pairing with sequence source only raises source oracle `0.401371 -> 0.401757`, so this is marginal. | Validation rows: `data/processed/validation_rows/yolo26l_cropmotion_samesum_w4_am02_thr085_same10_cross4_rootrate088_ma-004_mb008.csv`. Test CSV: `submissions/yolo26l_cropmotion_samesum_w4_am02_thr085_same10_cross4_rootrate088_ma-004_mb008_OFFLINE_CANDIDATE.csv`. Repro config: `tools/make_crop_motion_context_submission.py --tracks-dir data/processed/pose_tracks/test_yolo26l_conf035 --threshold 0.85 --nms-frames 10 --cross-nms-frames 4 --count-mode root_rate --count-multiplier 0.88 --context-feature same_sum --context-window 4 --context-alpha -0.2 --resize-width 320 --motion-alpha -0.04 --motion-beta 0.08 --jobs 16 --quiet`. | Weak independent timing tie-breaker; only useful inside ensemble/vote, not as direct submit. | Validated locally; do not submit alone. |
| opposite-fighter rival flip | Fixed-row identity audit showed role/color flips are dead, but a narrow nearby opposite-fighter pose rival rule is weakly positive across sources: yolo26l `0.365177 -> 0.365447`, sequence `0.379682 -> 0.380125`, gated hybrid `0.389125 -> 0.389672`. Retesting on the current best motion-gated sequence source gives only `0.401483 -> 0.401575` with 20 changed OOF rows. Full source oracle with base/seq/motion/rival reaches `0.402103`, with rival uniquely winning `agn_024`, `agn_025`, `agn_070`. | Audits: `notes/fighter_identity_error_audit_2026-05-21.md`, `notes/fighter_identity_rival_micro_current_best_2026-05-21.md`. OOF rows: `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_motion_rival_micro_oof.csv`. Test CSVs: older public-anchor `submissions/hybrid_yolo26l_best_agn038_seq_tcn_snap4_rootcount088_rival_w0_samehand_r13_min1_fht_OFFLINE_CANDIDATE.csv`; current full source `submissions/seq_tcn_yolo26x_witness_repeat_thr06_nms10_cross2_snap4_rootcount088_exchange_attr_motion_rival_micro_OFFLINE_CANDIDATE.csv`; conservative no-`agn_038` splice `submissions/hybrid_root_seqrepeat_exchange_attr_motion_rival_micro_noagn038_OFFLINE_CANDIDATE.csv`; current test CSVs validated with `tools/validate_data.py`. | High-precision identity micro-signal for future ensemble, not a standalone branch. | Not submitted; current-best gain is only `+0.000092`, too small under current guardrail. |
| learned exchange-side fighter flip | Learned OOF flip classifier over nearby self/rival pose candidates improves the current best source only `0.401483 -> 0.401720`, with 21 changed rows. | Audit: `notes/exchange_side_fighter_model_2026-05-21.md`. Tool: `tools/evaluate_exchange_side_fighter_model.py`. OOF rows: `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_motion_exchange_side_logreg_oof.csv`. | Weak identity diagnostic, possibly useful only as a future stack feature. | Do not submit or generate test CSV; below promotion gate. |
| exchange-state drop gate | Fixed-row no-punch gate helps base (`0.365177 -> 0.370306`), sequence (`0.379682 -> 0.383814`), and hybrid+rival (`0.389672 -> 0.390962`) while plain hybrid does not improve. Full source oracle with base/seq/motion/hybrid-rival/exchange reaches `0.403225`; exchange uniquely wins `agn_024` and `agn_025`. | Audit: `notes/exchange_state_gate_2026-05-21.md`. OOF rows: `data/processed/validation_rows/hybrid_yolo26l_seq_tcn_snap4_gated_rival_exchange_hgb_p024_oof.csv`. Test CSVs: public-anchor version `submissions/hybrid_yolo26l_best_agn038_seq_tcn_snap4_rootcount088_rival_exchange_hgb_p024_OFFLINE_CANDIDATE.csv`; private-risk gated analog `submissions/hybrid_yolo26l_best_seq_tcn_snap4_gate_oof395_rival_exchange_hgb_p024_OFFLINE_CANDIDATE.csv`; both validated. | FP/no-punch filter and source-gating signal. | Not submitted; OOF lift is modest and test gates are nearly inactive at the validated threshold. |
| fixed-row attribute model | Attribute-only OOF replacement on the hybrid+rival+exchange source improves `0.390962 -> 0.396329`; the main lift is `effectiveness` (`0.189015 -> 0.239715`). Effectiveness-only is the safer local variant at `0.395018` with fewer changes. Reapplying the same evaluator on the current `audio_tracklet` best source regresses every variant; best is `hand_target=0.405045` versus `0.405200`. | Audit: `notes/fixed_row_attribute_model_2026-05-21.md`. OOF rows: `data/processed/validation_rows/hybrid_yolo26l_seq_tcn_snap4_gated_rival_exchange_attr_all_oof.csv` and `..._attr_effectiveness_oof.csv`. Test CSVs: public-anchor `..._attr_all_OFFLINE_CANDIDATE.csv` / `..._attr_effectiveness_OFFLINE_CANDIDATE.csv`; private-risk gated `..._attr_all_OFFLINE_CANDIDATE.csv` / `..._attr_effectiveness_OFFLINE_CANDIDATE.csv`; all validated. | Independent older attribute branch, not a postprocess for the current best source. | Not submitted; promising only as a separate attribute-only public-anchor/private-risk candidate. |
| sequence repeat + exchange + attributes | Fixed repeat of the snap4/rootcount sequence spotter improved the full-sequence OOF source to `0.391357`; adding exchange gate and all-attribute replacement reached `0.400288`. Gating this source over the old attribute source reaches the current local best `0.400603`. Source oracle with old attribute sources and sequence-repeat variants reaches `0.418678`. | Audit: `notes/seq_repeat_exchange_attr_2026-05-21.md`. OOF rows: `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_all_oof.csv`. Test CSVs: full source `submissions/seq_tcn_yolo26x_witness_repeat_thr06_nms10_cross2_snap4_rootcount088_exchange_attr_all_OFFLINE_CANDIDATE.csv`; gated splice `submissions/hybrid_attrall_seqrepeat_exchange_attr_gate_OFFLINE_CANDIDATE.csv`; public-conservative no-`agn_038` splice `submissions/hybrid_root_seqrepeat_exchange_attr_gate_noagn038_OFFLINE_CANDIDATE.csv`; all validated. | Strong local source for future video-gated splice/ensemble. | Not submitted; best local splice changes `672` rows vs root, and the conservative no-`agn_038` splice still lacks fresh public evidence. |
| fixed-row crop-motion gate | A lightweight RGB/crop-motion keep/drop gate over the sequence-repeat exchange+attribute rows improves `0.400288 -> 0.401483`, dropping only 7 OOF rows. Source oracle with this branch reaches `0.418936`. | Audit: `notes/fixed_row_motion_gate_2026-05-21.md`. OOF rows: `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_motion_gate_oof.csv`. Test CSVs: full source `submissions/seq_tcn_yolo26x_witness_repeat_thr06_nms10_cross2_snap4_rootcount088_exchange_attr_motion_gate_OFFLINE_CANDIDATE.csv`; conservative no-`agn_038` splice `submissions/hybrid_root_seqrepeat_exchange_attr_motion_gate_noagn038_OFFLINE_CANDIDATE.csv`; both validated. | Best local source branch and short-clip/contact witness. | Not submitted; OOF lift is small and test artifacts still inherit sequence public risk. |
| old-attribute source switch | Simple fight-level rule `old_attr.source_le15>=0.93` over the current best local source gives `0.401483 -> 0.410780`; OOF rows are saved at `data/processed/validation_rows/seq_motion_old_attr_sourcele15_093_oof.csv`. The direct test analog validates at `submissions/seq_motion_old_attr_sourcele15_093_OFFLINE_CANDIDATE.csv` and selects `agn_038,agn_062,agn_063`. | Audit: `notes/source_switch_old_attr_rule_2026-05-21.md`; diagnostic table `data/processed/diagnostics/source_switch_old_attr_test_20260521.csv`; validated with `tools/validate_data.py`; group-OOF checked with `tools/evaluate_source_decision_stumps.py`. | Source-policy candidate for a future ensemble. | Not submitted; lift is dominated by `agn_003`, without that video the rule regresses, group-OOF is below base, and the test analog touches public-sensitive `agn_038`. |
| fixed-row audio gate | Audio-only remains dead, but adding audio onset/peak features to the pose/context keep-drop gate improves the current best local source `0.401483 -> 0.404479`; pose-only control is only `0.401999`. HGB is stable around thresholds `0.42..0.52`, logreg fails. | Audit: `notes/fixed_row_audio_gate_2026-05-21.md`. OOF rows: `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_motion_audio_gate_oof.csv`. Test CSVs: full source `submissions/seq_tcn_yolo26x_witness_repeat_thr06_nms10_cross2_snap4_rootcount088_exchange_attr_motion_audio_gate_OFFLINE_CANDIDATE.csv` and conservative `submissions/hybrid_root_seqrepeat_exchange_attr_motion_audio_gate_noagn038_OFFLINE_CANDIDATE.csv`; both validated with `tools/validate_data.py`. | Independent weak audio/pose gate for ensemble. | Not submitted; full artifact inherits sequence public risk, conservative artifact preserves `agn_038` but still lacks public evidence. |
| audio-gate exchange-side identity micro-source | Retesting fighter-only exchange-side flips on `audio_gate` gives `0.404479 -> 0.404915`, changing 22 OOF rows. The fighter oracle remains much higher at `0.419402`, but the learned recoverable piece is small. | Audit: `notes/audio_gate_exchange_side_identity_2026-05-21.md`. OOF rows: `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_motion_audio_gate_exchange_side_oof.csv`. Test CSVs: full audio gate `submissions/seq_tcn_yolo26x_witness_repeat_thr06_nms10_cross2_snap4_rootcount088_exchange_attr_motion_audio_exchange_side_OFFLINE_CANDIDATE.csv` and protected `submissions/hybrid_root_seqrepeat_exchange_attr_motion_audio_exchange_side_noagn038_OFFLINE_CANDIDATE.csv`; both validate and change 11 fighter labels. | Tiny independent fighter-identity signal. | Save for ensemble only; no upload from this branch alone. |
| tracklet appearance fighter micro-source | Per-video tracklet appearance/equipment descriptors add a tiny fixed-row fighter signal. On `audio_gate`, HGB threshold `0.6` improves `0.404479 -> 0.404679` with 2 flips; on the `audio_gate_exchange_side` source it improves `0.404915 -> 0.405200` with one flip (`agn_003 frame=2421 blue->red`). Fight-level prototype pooling regresses to `0.404856`. | Audit: `notes/tracklet_appearance_fighter_identity_2026-05-21.md`. Tool: `tools/evaluate_tracklet_appearance_fighter_model.py`. OOF rows: `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_motion_audio_exchange_tracklet_appearance_oof.csv`. | Previous best local OOF row source and independent tracklet/equipment identity micro-signal. | Save for ensemble only; no test CSV or upload from this branch alone. |
| RGB fixed-row effectiveness witness | Cached ViT-B/16 clip features over the full RGB bridge candidate pool improve only fixed-row `effectiveness` on top of the `audio_tracklet` source. Ungated effectiveness reaches `0.406345`; confidence margin `0.2` improves to `0.407232` with 367 effectiveness changes. On the nearest test-capable `audio_exchange` source, the same margin gives `0.404915 -> 0.406939`; RGB margin checks for `punch_type`, `hand`, and `target` still regress. | Audits: `notes/rgb_fixed_row_attribute_witness_2026-05-21.md`, `notes/rgb_fixed_row_attribute_submission_2026-05-21.md`. Tools: `tools/evaluate_rgb_fixed_row_attribute_model.py`, `tools/make_rgb_fixed_row_attribute_submission.py`. OOF rows: `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_motion_audio_exchange_tracklet_rgb_effectiveness_margin02_oof.csv` and `data/processed/validation_rows/seq_tcn_snap4_rootcount088_repeat_exchange_attr_motion_audio_gate_exchange_side_rgb_effectiveness_margin02_oof.csv`. Test CSVs: full `submissions/seq_tcn_yolo26x_witness_repeat_thr06_nms10_cross2_snap4_rootcount088_exchange_attr_motion_audio_exchange_side_rgb_eff_m02_OFFLINE_CANDIDATE.csv` and protected `submissions/hybrid_root_seqrepeat_exchange_attr_motion_audio_exchange_side_rgb_eff_m02_noagn038_OFFLINE_CANDIDATE.csv`; both validate. | New best local OOF row source and independent RGB effectiveness signal. | Save for ensemble/private-risk bookkeeping only; no upload without explicit approval. |
| public-anchor RGB effectiveness | The same cached RGB effectiveness head improves the public-safe yolo26l anchor `0.365177 -> 0.371229` ungated and `0.369373` at margin `0.2`, while preserving timing/count/fighter. | Audit: `notes/rgb_public_anchor_effectiveness_2026-05-21.md`. OOF rows: `data/processed/validation_rows/yolo26l_samesum_w4_am02_thr085_same10_cross4_rootrate088_rgb_effectiveness_margin0_oof.csv` and `data/processed/validation_rows/yolo26l_samesum_w4_am02_thr085_same10_cross4_rootrate088_rgb_effectiveness_margin02_oof.csv`. Test CSVs: `submissions/hybrid_yolo26l_best_agn038_seq_tcn_snap4_rootcount088_rgb_eff_m0_OFFLINE_CANDIDATE.csv` and `submissions/hybrid_yolo26l_best_agn038_seq_tcn_snap4_rootcount088_rgb_eff_m02_OFFLINE_CANDIDATE.csv`; both validate. | Public-safe attribute-only RGB probe. | No automatic upload; superseded by the stacked public-anchor attribute+RGB branch. |
| stacked public-anchor attribute + RGB effectiveness | RGB effectiveness stacks positively on the older public-anchor attribute models. `attr_all` improves `0.396329 -> 0.398315` at margin `0.3`; effectiveness-only improves `0.395018 -> 0.397369` at margin `0`. | Audit: `notes/rgb_stacked_public_anchor_attributes_2026-05-21.md`. OOF rows: `data/processed/validation_rows/hybrid_yolo26l_seq_tcn_snap4_gated_rival_exchange_attr_all_rgb_effectiveness_margin03_oof.csv` and `data/processed/validation_rows/hybrid_yolo26l_seq_tcn_snap4_gated_rival_exchange_attr_effectiveness_rgb_effectiveness_margin0_oof.csv`. Test CSVs: full `attr_all`/`attr_effectiveness` artifacts plus `agn_038`-only splices `submissions/hybrid_root_attr_effectiveness_rgb_eff_m0_agn038_only_OFFLINE_CANDIDATE.csv` and `submissions/hybrid_root_attr_all_rgb_eff_m03_agn038_only_OFFLINE_CANDIDATE.csv`; all validate. | Stronger fixed-row public-anchor attribute branch; effectiveness-only variant changes only `effectiveness` versus root. | No automatic upload; this supersedes raw public-anchor RGB effectiveness as the cleanest public-safe fixed-row candidate. |

## Watchlist, Not Direct Candidates

| Branch | Reason to keep | Kill condition |
| --- | --- | --- |
| yolo26x/yolo11s agreement | Good OOF before public, but public full submit was `0.10784`; may still be useful as an agreement/witness feature around selected events. | Do not use as row source unless a video-local gate proves it. |
| audio onset features | Audio-only and hard snap are killed, but local onset max/distance now has a positive fixed-row pose/audio gate result (`0.404479`) when used as auxiliary features only. | Do not generate audio-only rows or hard-shift selected frames to audio peaks; keep audio inside learned pose-dominant gates/rerankers. |
| fighter identity visual embeddings | Existing HSV/bbox clustering and simple role/color flips failed. A tracklet-appearance fixed-row HGB now has a tiny positive fighter result (`0.405200` before RGB effectiveness), but only from one validation flip. | Keep only if future fixed-timing validation improves fighter score by more than a micro-delta without hurting time/count. |
| candidate-level RGB contact | A first ViT-B/16 temporal clip smoke over a raw yolo26x pose candidate pool improves same-pool OOF `0.330787 -> 0.359641` and reduces FP penalty, so RGB has real pre-NMS signal. Absolute score remains far below the current best `0.407232`. | Do not submit as a raw-pose branch. Keep only as auxiliary candidate-ranking infrastructure. |
| sequence-TCN + RGB contact bridge | `tools/evaluate_pose_sequence_spotter.py` now has an optional RGB-contact blend before final selection. A tiny infrastructure smoke passed, cap-400 reached `0.388112`, and full pool-1800 A/B showed RGB helps inside the sequence scorer (`0.386043 -> 0.391237`), but the final source stayed far below the current best `0.407232`. | Do not submit or create test CSV from this branch. Keep the cache/tooling only as future witness infrastructure. |
| sequence-TCN + audio contact bridge | `tools/evaluate_pose_sequence_spotter.py` now has an optional audio-contact blend before final selection. Medium cap-400 run shows audio helps the weak sequence setup (`0.376648 -> 0.379792` with `audio_alpha=0.25`) but stays below the current best `0.407232` and the full RGB bridge `0.391237`. | Keep infrastructure; do not submit or create test CSV from cap-400. |
| row source oracle with RGB/audio/tracklet additions | Adding the latest `rgb_eff_m02`, `rgb_eff`, `audio_tracklet`, and full RGB bridge sources raises per-video source oracle to `0.430014`; `rgb_full` and `old_attr` still have strong pairwise oracle headroom. Existing fight-level selectors still fail OOF (`stumps=0.397342`, `hgb=0.404206`, base `0.407232`). | Keep sources for future ensemble research; do not use current automatic source-policy stack for submit. |

## Killed During Ensemble Audit

| Branch | Evidence | Decision |
| --- | --- | --- |
| row-level source stacker over saved CSVs | `tools/evaluate_row_source_stacker.py` OOF best was only `0.368373` using the union of base/seq/rival/exchange/attr/motion rows, far below `attr_all=0.396329`. Source-policy retest with attributes produced `source_oracle=0.408357`, but learned policies were worse than simply choosing `attr_all`: `mean_root=0.391194`, `ridge=0.372111`, `hgb=0.366116`. | Keep the diagnostic tool, but do not use current row-source stacking as a submit or ensemble branch. |
| learned per-video count controller | Direct yolo26x count control ties but does not beat the best static root-round count policy (`ridge/hgb=0.374335` vs `root_round_count=0.374359`). yolo26x/yolo26l agreement count control regresses badly (`0.339953`, FP `0.171750`). | Kill as a growth path for the current candidate pools; count regression is not the missing fight-level signal. |
| learned tabular temporal selector on yolo26l candidates | `logs/yolo26l_learned_selector_20260521.log` best local score was `0.275094`, with high FP penalty `0.153317`, below the yolo26l heuristic and far below current attribute/exchange sources. | Killed without richer features; do not spend more cycles on the current label/features/postprocess setup. |
| temporal frozen RGB contact on fixed rows | `tools/evaluate_rgb_contact_clip.py` tested frozen ViT-B/16 clip embeddings (`N x 8 x 768`) with a small OOF GRU head on the current best source. Union crop regressed to `0.385960` for keep/drop and `0.398441` for offset-only; attacker/opponent crop regressed to `0.398062` for keep/drop and `0.398528` for offset-only, below the `0.401483` baseline. | Kill as a fixed-row direct branch. Keep the tool only for a future candidate-level or fine-tuned RGB experiment. |
| RGB fixed-row fighter flip model | Cached row-aligned ViT-B/16 clip features were retargeted from contact filtering to fighter flips. Union-crop HGB produced only a two-row micro gain (`0.401483 -> 0.401489`); attacker/opponent crop regressed (`0.401078` best). | No OOF/test CSV generated; do not use as a submission branch. |
| deep crop per-video fighter identity calibration | ResNet50 crop embeddings sometimes produce high per-video cluster purity, but fixed candidate remapping regressed: baseline `0.361655`, gated map `0.356873`, oracle cluster map `0.342864`. | Kill this unsupervised track-cluster remapping path; even validation oracle mapping is worse. |
